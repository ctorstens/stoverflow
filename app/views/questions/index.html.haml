%h1.header
  StoverFlow

- @questions.each do |question|
  %p
    Score
    =question.votes.map{|vote| vote.value }.inject(0) {|sum,v| sum + v}
  %h4=link_to question.title.titleize, question_path(question), :method => :get