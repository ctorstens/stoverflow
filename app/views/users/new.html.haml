%h2 Sign Up For Stoverflow

= form_for @user do |f|
  = f.label(:username)
  = f.text_field(:handle, placeholder: 'eg. braveLittleToaster')

  = f.label(:email)
  = f.email_field(:email, placeholder: 'eg. toaster@email.com')
  
  = f.label(:password)
  = f.password_field(:password, placeholder: 'eg. t0ast3d')
  = f.submit('Sign Up')
